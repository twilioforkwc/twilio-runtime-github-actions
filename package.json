{
  "name": "twilio-ci-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "axios": "^0.19.2",
    "jest": "^26.6.3",
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": ">=8.0.0 <11.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
